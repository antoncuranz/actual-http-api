name: Docker Image Publish

on:
  push:
    branches: [ "main" ]
    tags: [ '*.*.*' ]
  # Removed pull_request trigger to avoid running CI on Dependabot PRs

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GHCR
        if: ${{ !github.event.repository.fork }}
        uses: docker/login-action@v3.1.0
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ github.token }}

      - name: Build Docker image
        if: ${{ github.event.repository.fork }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: false
          tags: ghcr.io/antoncuranz/actual-http-api:latest

      - name: Build and push Docker image
        if: ${{ !github.event.repository.fork }}
        id: build-and-push
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ghcr.io/antoncuranz/actual-http-api:latest
